import 'package:flutter/material.dart';

void main() {
  runApp(const MyVideoShareApp());
}

class MyVideoShareApp extends StatelessWidget {
  const MyVideoShareApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}